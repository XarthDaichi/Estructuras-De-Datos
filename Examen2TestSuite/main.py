import string, binary_tree
if __name__ == '__main__':
    # hashtable = {}
    # text = open("archivo.txt", "r")
    # for line in text:
    #     for temp_word in line.split():
    #         word = temp_word.translate(str.maketrans('', '', string.punctuation))
    #         if word in hashtable:
    #             hashtable[word] += 1
    #         else:
    #             hashtable[word] = 1
    # sorted_hashtable = dict(sorted(hashtable.items()))
    # for key in sorted_hashtable.keys():
    #     print(key , '\t', sorted_hashtable[key])
    pass